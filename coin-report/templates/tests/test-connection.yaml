apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coin-report.fullname" . }}-test-connection"
  labels:
    {{- include "coin-report.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coin-report.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
